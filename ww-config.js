export default {
  editor: {
    label: {
      en: "App",  // Change as per your component name
    },
  },
  properties: {
    pageSize: {
      label: {
        en: "Page Size",
      },
      type: "Number",
      defaultValue: 10,
    },
    allowPaging: {
      label: {
        en: "Allow Paging",
      },
      type: "Boolean",
      defaultValue: true,
    },
    // Add more properties as needed based on Syncfusion configuration options
  },
};
